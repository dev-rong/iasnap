import { Metadata } from "next";
import Partners from "./page";

export const generateMetadata = (): Metadata => {
  return {
    title: "이아스냅 제휴업체 | IA SNAP",
    description: "이아스냅의 제휴업체입니다."
  };
};

export default Partners;